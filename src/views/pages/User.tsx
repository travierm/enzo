import { Layout } from "../components/Layout";

// import * as React from "react";

// type User = {
//   name: string;
// };

export function User(props: {}) {
  return (
    <Layout>
      <ul>test user</ul>
    </Layout>
  );
}
